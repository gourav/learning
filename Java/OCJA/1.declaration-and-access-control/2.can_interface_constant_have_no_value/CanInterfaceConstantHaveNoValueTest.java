/**
 * This test is simply to verify if we can have an interface that though defines a constants but does not initializes it.
 */
public interface CanInterfaceConstantHaveNoValueTest {

	int MAX_VALUE;

} 
