#include <stdio.h>

int main() {

    _Bool is_today_sunday = 2;
    if( is_today_sunday ) {
        printf( "It's sunday today. " );
    } else {
        printf( "It's not sunday today." );
    }

    return 0;

}
